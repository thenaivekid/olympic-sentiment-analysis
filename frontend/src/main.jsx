import React from 'react'
import ReactDOM from 'react-dom/client'
import AppRemake from './AppRemake.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppRemake />
  </React.StrictMode>,
)
